(define (linear-equation a11 a12 a21 a22 b1 b2)
  (define delta (- (* a11 a22) (* a12 a21)))
  (define delta_x (- (* b1 a22) (* a12 b2)))
  (define delta_y (- (* a11 b2) (* b1 a21)))
  (if(= delta 0)
     (list)
     (list (/ delta_x delta) (/ delta_y delta))
     )
  )
